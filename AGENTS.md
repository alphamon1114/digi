# 작업 규칙

- 먼저 `docs/GAME_DESIGN.md`, `docs/DECISIONS.md`, `docs/HANDOFF.md`를 읽는다.
- 작업 대상 Unity 프로젝트는 `digi/`이다. `My project/`는 기존 별도 프로젝트로 보존한다. 저장소 루트 자체는 Unity 프로젝트가 아니다.
- 새로운 기능, 동작·밸런스·구조 변경은 관련 기획, 결정, 변경 이력, 인수인계 문서를 함께 갱신한다.
- 사용자 확정 기획, 개발상 임시 선택, 미해결 질문을 구분한다. 사용자 지시 없이 핵심 장르·규칙을 바꾸지 않는다.
- 게임 상태를 바꾸는 입력은 서버에서 검증한다. 악당 전용 정보는 수신자별로 필터링한다.
- 작업 종료 전 구현 내용, 실제 실행한 검증과 실행하지 못한 검증, 다음 작업을 기록한다.
- 기존 작업과 에셋을 보존한다. 신규 Unity 에셋과 폴더의 `.meta`를 함께 관리한다.
- 문서의 저장소 경로는 상대 경로로 쓴다. 개인 환경 경로·비밀정보를 넣지 않는다. 원격 push는 사용자 요청이 있을 때만 한다.
